#ifndef INITSDL_H
#define INITSDL_H

#include <string>

#include <SDL2/SDL.h>
#include "ModelData.h"
#include <string>


class InitSDL
{
public:
    InitSDL();
    virtual ~InitSDL();
    InitSDL(const InitSDL&) = delete;
    void operator=(const InitSDL&) = delete;

private:
    
};

#endif
